/**
 * UI do relay wwebjs (QR/conexão) permanece desligada.
 * Transporte de produção: Twilio (`aires.whatsapp.twilio.habilitado` na API).
 * O desk de chat está em `/dashboard/atendimento/chat`.
 */
export const WHATSAPP_RELAY_UI_ENABLED = false;
